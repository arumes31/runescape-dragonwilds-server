name: Scheduled container audit
on:
  schedule:
    - cron: '41 4 * * 1'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: scheduled-container-audit
  cancel-in-progress: false
jobs:
  verify:
    uses: ./.github/workflows/verify.yml
